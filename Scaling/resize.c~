#include <gtk/gtk.h>
#include <stdlib.h>
#include <stdio.h>


int main(int argc, char *argv[])
{
  
  GtkWidget *widget = gtk_window_new(GTK_WINDOW_TOPLEVEL);
  g_signal_connect(widget, "delete-event", G_CALLBACK(gtk_main_quit),NULL);
  gtk_widget_show_all(widget);
  

  return 0;


}
