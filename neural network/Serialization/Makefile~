## Simple SDL mini code
 
CC=gcc
 
CPPFLAGS= `pkg-config --cflags` -MMD
CFLAGS= -Wall -Wextra -Werror -std=c99 -O3
LDFLAGS=
LDLIBS= `pkg-config --libs` -lm
 
OBJ= main.o 
DEP= ${SRC:.o=.d}
 
all: main
 
main: ${OBJ}
 
clean:
	${RM} ${OBJ} ${DEP} *.d
	${RM} main 
 
-include ${DEP}
 
# END
